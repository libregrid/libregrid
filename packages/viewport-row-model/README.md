# @libregrid/viewport-row-model

The push-driven Viewport Row Model for LibreGrid. Register
`ViewportRowModelModule` and provide `viewportDatasource`; its `init` callback
receives `setRowCount`, `setRowData`, and `getRow` functions.
