{
  "name": "viewport-row-model",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["type:framework-neutral"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p tsconfig.lib.json", "cwd": "packages/viewport-row-model" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/viewport-row-model" } }
  }
}
