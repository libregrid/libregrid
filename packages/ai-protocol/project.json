{
  "name": "ai-protocol",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["type:framework-neutral"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "node ../../tools/clean-package-dist.mjs ai-protocol && tsc -p tsconfig.lib.json", "cwd": "packages/ai-protocol" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run packages/ai-protocol" }
    }
  }
}
