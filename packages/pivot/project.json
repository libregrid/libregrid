{
  "name": "pivot",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["type:framework-neutral"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p tsconfig.lib.json", "cwd": "packages/pivot" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/pivot" } }
  }
}
