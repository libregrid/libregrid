---
"@libregrid/excel-export": minor
---

Add formulas and export callbacks (Phase 5.7): `autoConvertFormulas`, `processCellCallback`, `processHeaderCallback`, `processGroupHeaderCallback`, `processRowGroupCallback`, `shouldRowBeSkipped`, `getCustomContentBelowRow`, `transformValues` and `valueFrom`. Column groups export as merged header rows.
