---
"@libregrid/excel-export": minor
---

Add grouping outlines (Phase 5.5): row and column `outlineLevel` with row `collapsed` and `hidden` state, so grouped-grid exports keep their expandable outline levels.
