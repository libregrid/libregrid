---
"@libregrid/excel-export": minor
---

Add the Phase 5.1 OOXML SpreadsheetML writer skeleton. The package assembles a `.xlsx` ZIP archive with `fflate`, writes the core workbook parts, deduplicates shared strings, and ships an unzip-and-assert test harness with golden fixtures.
